# Model artifacts

Training writes `.keras` models and run metadata here. Large files are ignored by Git. Publish presentation-ready models through GitHub Releases or cloud object storage and document their checksum and download URL.
